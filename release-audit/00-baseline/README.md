# RC audit baseline

This directory records the immutable starting facts for the final pre-device audit. The baseline intentionally includes the uncommitted Voice, Calibration, and Motion work that preceded this audit. No secret values or local environment files are captured.

The recorded test results are the immediately preceding full verification run. Every release gate is rerun after the six audits and again from the resulting RC commit.
